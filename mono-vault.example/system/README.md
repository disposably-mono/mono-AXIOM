# system/

System-level configuration: routing rules, schemas, provider settings.

Subdirectories (created as needed):
- `config/`  — runtime config for each layer
- `routing/` — intent-to-provider routing rules
- `schemas/` — formal schemas for new vault content types
